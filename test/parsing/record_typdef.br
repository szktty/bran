type r = { a : int }
type r = { a : int, }

type r = { a : int, b : float, mutable c : string }
type r = { a : int, b : float, mutable c : string, }

type r =
  { a : int }

type r = {
  a : int,
  b : float,
  c : string
}
